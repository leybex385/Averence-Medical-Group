
/* Simple Dropdown */
.nav-item.simple-dropdown { position: relative; }
.dropdown-menu {
    position: absolute;
    top: 100%;
    left: 50%;
    transform: translateX(-50%) translateY(10px);
    background: #fff;
    padding: 10px 0;
    min-width: 220px;
    border-radius: 6px;
    box-shadow: 0 15px 40px rgba(0,0,0,0.1);
    opacity: 0;
    visibility: hidden;
    transition: all 0.3s ease;
    pointer-events: none;
    z-index: 100;
}
.nav-item.simple-dropdown:hover .dropdown-menu {
    opacity: 1;
    visibility: visible;
    transform: translateX(-50%) translateY(0);
    pointer-events: auto;
}
.dropdown-menu a {
    display: block;
    padding: 12px 20px;
    color: var(--primary-navy);
    font-size: 0.95rem;
    font-weight: 500;
    transition: all 0.2s ease;
    text-decoration: none;
}
.dropdown-menu a:hover {
    background: rgba(18, 48, 74, 0.03);
    color: var(--accent-cyan);
}
